This folder is reserved for custom showroom shaders. The first production pass uses
physically based GLB materials, HDRI reflections, bloom, vignette, and mesh-based red
floor glow so the car remains replaceable without shader-specific model edits.
