{
  "name": "moto-cooperation",
  "version": "1.0.0",
  "private": true,
  "description": "Moto Cooperation — Car & Bike Community Platform with Admin Dashboard",
  "author": "Moto Cooperation",
  "license": "MIT",
  "scripts": {
    "start": "cd backend && npm install && npm start",
    "dev": "cd backend && npm start",
    "install-all": "npm install && cd backend && npm install && cd ..",
    "vercel-build": "echo 'Build complete'",
    "build": "echo 'Build complete'"
  },
  "engines": {
    "node": ">=14.0.0"
  },
  "dependencies": {
    "express": "^4.18.2",
    "multer": "^2.1.1",
    "serverless-http": "^3.2.0"
  },
  "devDependencies": {}
}
